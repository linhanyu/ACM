//
// Created by Henry on 16/9/15.
//
#include <cstdio>
#include <algorithm>
#include <functional>
using namespace std;

const int MAXN = 12;
int A[MAXN];
bool used[MAXN];
int n,t;

int l,start;
bool dfs(int sum){

    if (sum == 0 )return true;

    start = 0;
    if () start = l;
    for (int i = start; i < n; ++i) {
        if ()
    }
}

int main(){
    while(scanf("%d %d",&t,&n)!=EOF && (n || t)){
        int total=0
        for (int i = 0; i < n; ++i) {
            scanf("%d",&A[i]);
            total += A[i];
        }

        sort(A,A+n,greater<int>());


    }
}